class Chat {
  final String name, lastMessage, image, time, chatId;
  final bool isActive;

  Chat({
    this.name,
    this.chatId,
    this.lastMessage,
    this.image,
    this.time,
    this.isActive,
  });
}
